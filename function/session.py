import os
import sys
import tensorflow.compat.v1 as tf
import numpy as np
import glob
import math
import csv
import networkset
from datetime import datetime
import time
from tqdm import tqdm
import shutil

# tfrecordsの解析読み込み関数
def parse_function(data):
    # ファイル形式次第
    return parsed_features  # 解析したデータを返す

    
